plugins {
    id 'org.gradle.guides.getting-started' version '0.4.1'

    // Uncomment this line if you need test JVM code snippets
    // id 'org.gradle.guides.test-jvm-code' version '0.4.1'
}

import org.apache.tools.ant.filters.*

guide {
    repoPath = "gradle-guides/@@GUIDE_SLUG@@"
}

// Add in any generation steps.
gradleRunner {

}
