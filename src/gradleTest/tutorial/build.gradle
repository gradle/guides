apply plugin : 'org.gradle.guides.tutorial'
apply plugin : 'org.gradle.guides.test-jvm-code'

guide {
    repoPath 'foo/bar'
}

gradleRunner {
    // Purposefully left empty
}

task runGradleTest {
  dependsOn asciidoctorAttributes
}
