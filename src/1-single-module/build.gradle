subprojects {
    afterEvaluate {
        repositories {
            jcenter()
        }

        // tag::autoModuleName[]
        jar {
            manifest {
                attributes('Automatic-Module-Name': moduleName)
            }
        }
        // end::autoModuleName[]
    }
}
