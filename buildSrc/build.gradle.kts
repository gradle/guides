plugins {
    id("org.jetbrains.kotlin.jvm") version "1.2.41"
    id("org.gradle.kotlin.kotlin-dsl") version "0.18.1"
    java // javaModule script also applies this, but applying it here means we get nicer DSL syntax below
}
